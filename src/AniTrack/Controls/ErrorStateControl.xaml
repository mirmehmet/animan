<UserControl x:Class="AniTrack.Controls.ErrorStateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="140" d:DesignWidth="360"
             x:Name="Root"
             Visibility="Collapsed">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                AutomationProperties.LiveSetting="Assertive"
                AutomationProperties.Name="{Binding ErrorMessage, ElementName=Root}">

        <ui:SymbolIcon Symbol="ErrorCircle24"
                       FontSize="32"
                       HorizontalAlignment="Center"
                       Foreground="#EF4444"/>

        <TextBlock Text="{Binding ErrorMessage, ElementName=Root}"
                   FontSize="12"
                   Margin="0,8,0,0"
                   MaxWidth="320"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

        <!-- Retry button — hidden when RetryCommand is null -->
        <ui:Button Margin="0,12,0,0"
                   HorizontalAlignment="Center"
                   Appearance="Secondary"
                   Content="{Binding RetryText, ElementName=Root}"
                   Command="{Binding RetryCommand, ElementName=Root}"
                   AutomationProperties.Name="{Binding RetryText, ElementName=Root}">
            <ui:Button.Style>
                <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RetryCommand, ElementName=Root}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ui:Button.Style>
        </ui:Button>

    </StackPanel>
</UserControl>
